-- Historical releases provisioned one deployment-specific external project
-- here. Fresh installations must start with the documented empty project
-- registry, while installations that already applied 059 keep their existing
-- row and administrator configuration. Intentionally no-op.
SELECT 1;
